{
  "name": "course-terrain-tool",
  "version": "1.0.0",
  "description": "An open source tool to find and process terrain data for use in the OPCD golf course design process",
  "workspaces": [
    "client",
    "app"
  ],
  "main": "app/main.js",
  "build": {
    "name": "Course Terrain Tool",
    "npmRebuild": false,
    "appId": "com.opengolfsim.ctt",
    "mac": {
      "category": "public.app-category.developer-tools"
    },
    "extraResources": [
      "./server",
      "./client/dist"
    ]
  },
  "scripts": {
    "app:dir": "electron-builder --dir",
    "app:dist": "electron-builder",
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "CLIENT_DEV_MODE=1 concurrently --kill-others  'npm run debug --workspace=client' 'npm run -w app start'",
    "server": "npm start --workspace=server",
    "build": "npm run build --workspace=client",
    "develop": "CTC_DEBUG=1 concurrently --kill-others 'npm run debug --workspace=client' 'npm start --workspace=server'",
    "start": "npm start --workspace=server",
    "app_deps": "electron-builder install-app-deps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "express": "^4.21.2",
    "express-ws": "^5.0.2",
    "p-map": "^7.0.3",
    "sharp": "^0.33.5",
    "staticmaps": "^1.13.1",
    "dotenv": "^16.4.7"
  },
  "volta": {
    "node": "20.18.1"
  }
}
